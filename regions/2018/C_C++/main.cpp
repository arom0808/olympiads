#include <iostream>
#include <vector>

int main() {
    std::uint64_t humans_count, floors_count, humans_entered = 0;
    std::cin >> humans_count >> floors_count;
    std::vector<std::uint32_t> results{};
    results.resize(humans_count, 0);
    std::uint64_t cache_1, cache_2, cache_3;
    std::uint64_t id_of_first = 0;
    std::cin >> cache_1 >> cache_2;
    ++humans_entered;
    std::vector<std::pair<std::uint64_t, std::uint64_t >> humans;

    return 0;
}
