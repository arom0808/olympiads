#include <iostream>
#include <vector>
#include <set>

int main() {
    std::uint32_t N, K;
    std::cin >> N >> K;
    std::vector<std::uint32_t> stalls(N);
    for (auto &&stall: stalls)
        std::cin >> stall;
    Max
    return 0;
}
